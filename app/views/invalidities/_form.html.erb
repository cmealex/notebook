<%= form_for(@invalidity) do |f| %>
  <% if @invalidity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invalidity.errors.count, "error") %> prohibited this invalidity from being saved:</h2>

      <ul>
      <% @invalidity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.number_field :type %>
  </div>
  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
