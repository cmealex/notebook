<%= form_for(@user_invalidity) do |f| %>
  <% if @user_invalidity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_invalidity.errors.count, "error") %> prohibited this user_invalidity from being saved:</h2>

      <ul>
      <% @user_invalidity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userId %><br>
    <%= f.number_field :userId %>
  </div>
  <div class="field">
    <%= f.label :invalidityId %><br>
    <%= f.number_field :invalidityId %>
  </div>
  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
